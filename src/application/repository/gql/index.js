import repositories from './repositories';
import { repository, contributors } from './repository';

export default {
    queries: {
        repositories,
        repository,
        contributors
    },
};
